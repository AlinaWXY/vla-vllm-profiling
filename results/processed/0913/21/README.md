# 0913/21 — 实际框架源码的整模型回归

框架提交：`6bdbf97e357232b8aa3b6caf5a6c8b0fed713c25`；GPU：NVIDIA L20；Slurm job 1490577。
采集开始（含导入和模型加载）：2026-09-13T17:19:04+08:00；完成：2026-09-13T17:19:35+08:00。
报告生成：2026-09-13T17:22:26+08:00。

主测量直接调用实际修正源码的 `Pi05Pipeline.forward`，没有注入框架数值补丁。使用相同真实权重、seed=17、10 步去噪，比较最终 `[50,32]` 归一化动作。

| 输入条件 | 源码 optimized vs safe RMSE | 相对 RMSE | 最大绝对差 | 同进程修正控制最大差 |
| --- | ---: | ---: | ---: | ---: |
| 历史缺图复现 | 0.00207398 | 3.6133% | 0.00666749 | 0.00000000 |
| 三路有效图像 | 0.00134642 | 1.0047% | 0.00665259 | 0.00000000 |

三个框架回归测试通过：相机输出生命周期、模型频率表、Gemma 半区配对 fused QKV（位置 0、150、918）。项目隔离 checkout 的 24 项 CPU 测试通过。

全部 813 个参数名称加载完成，缺失 0。两个输入条件下，源码输出与同进程的“原始解码器 + 已验证 RoPE 修正”控制逐元素一致；图重放逐元素一致；safe 与原先保存的 safe 逐元素一致。相机覆盖修正另外由生命周期回归测试和 0913/15 的完整 pipeline 对照验证。

同进程控制与独立历史诊断分别报告：三路有效图像的当前源码相对于 0913/15 诊断组合修正输出，RMSE=0.00093346、最大差=0.00585938。之前 0913/16 将跨运行逐元素一致设为硬断言而退出，保留该记录。当前同进程等价测试证明源码落实一致；跨运行差异的执行状态/编译因素没有进一步单独归因，不把其来源写成已经证实。

约 1% 的残差仍存在，不能宣称达到外部 oracle/机器人任务容差。没有测量修正后性能。

原始指标：`source_validation.json`；最终动作：`source_validation_actions.npz`；独立数值复核：`verification.json`。资源与代码快照见 `submission.json`、`run.slurm`、`validate_framework.source.py`；框架测试与推理日志见 `slurm_1490577.log`。
