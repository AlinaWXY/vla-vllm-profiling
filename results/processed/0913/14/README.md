# 0913/14 — π0.5 整模型计算差异

硬件：NVIDIA L20；Torch：2.11.0+cu130。
完整 pipeline 采集：2026-09-13T16:44:04+08:00 至 2026-09-13T16:45:22+08:00。
报告生成：2026-09-13T16:45:25+08:00。

所有表格比较 `Pi05Pipeline.forward` 的最终 `[50,32]` 动作，覆盖预处理、图像编码、前缀编码及全部 10 步去噪，优化路径包含实际 CUDA Graph 捕获和重放。
同一真实 checkpoint、同一初始噪声、同一输入；batch=1、seed=17、BF16 权重和 FP32 初始噪声。
相对 RMSE = RMSE(optimized−safe) / RMS(safe)。这些是归一化动作输出，没有真实动作标签。

修正仅用于本次诊断进程；每次改动后重新捕获相关 CUDA Graph。没有修改上游源码或已有环境，也没有测量加速比。

## 历史缺图条件复现

图像 mask：[False, False, False]。

| 完整 pipeline 配置 | 绝对 RMSE | 相对 RMSE | 最大绝对误差 | 平均绝对误差 |
| --- | ---: | ---: | ---: | ---: |
| 原始优化版 | 0.05933472 | 103.3719% | 0.19186890 | 0.04365504 |
| 修正 RoPE 配对和频率 | 0.00207398 | 3.6133% | 0.00666749 | 0.00157863 |
| 再还原 safe 输出投影 / Euler / FP32 状态 | 0.00212228 | 3.6974% | 0.00683594 | 0.00158890 |
| 再匹配 eager RoPE 中间舍入 | 0.00211008 | 3.6761% | 0.00683594 | 0.00157072 |

safe 重复运行最大绝对差：0.00000000；各优化配置捕获后首轮与重放最大绝对差的最大值：0.00000000。

## 三路有效图像

图像 mask：[True, True, True]。

| 完整 pipeline 配置 | 绝对 RMSE | 相对 RMSE | 最大绝对误差 | 平均绝对误差 |
| --- | ---: | ---: | ---: | ---: |
| 原始优化版 | 0.22256906 | 166.0840% | 0.72293675 | 0.15265399 |
| 修正 RoPE 配对和频率 | 0.00417133 | 3.1127% | 0.02228448 | 0.00314640 |
| 再还原 safe 输出投影 / Euler / FP32 状态 | 0.00420138 | 3.1351% | 0.02207947 | 0.00316384 |
| 再匹配 eager RoPE 中间舍入 | 0.00412969 | 3.0816% | 0.02225494 | 0.00311707 |

safe 重复运行最大绝对差：0.00000000；各优化配置捕获后首轮与重放最大绝对差的最大值：0.00000000。

## 独立控制组与解释

加载审计：813 个参数名称已加载，缺失 0 个。
实际 RoPE 频率 dtype：expert `torch.bfloat16`，prefix `torch.bfloat16`。

- 历史缺图条件复现：只换优化前缀、保留 safe 解码器的相对 RMSE 2.7986%；safe 去除被 mask token 的相对 RMSE 2.0782%；safe 预计算条件的相对 RMSE 0.0000%。
- 三路有效图像：只换优化前缀、保留 safe 解码器的相对 RMSE 0.7929%；safe 去除被 mask token 的相对 RMSE 0.6963%；safe 预计算条件的相对 RMSE 0.0000%。

`numerics.json` 还单独记录了只改 RoPE 布局、只改 Euler、同时匹配模型频率等消融，以及每一步去噪误差和逐层 prefix KV 误差。它们共用 eager 图像 embeddings；上面主表则是完整 pipeline 实测。

误差下降不等于通过部署容差；不同修正之间有交互，不能把误差差值直接相加为各 bug 的贡献。
匹配 safe 的 BF16 频率只用于内部对齐；外部 OpenPI/LeRobot 正确性和机器人任务成功率尚未验证。

完整结果：`full_pipeline.json`、`full_pipeline_actions.npz`、`full_pipeline_comparison.csv`。
源码定位与单内核复现见 [0913/05 调查说明](../05/README.md)。
