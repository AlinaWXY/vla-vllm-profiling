# 0913/14 — 真实权重数值消融

实际采集：2026-09-13T16:43:36+08:00 至 2026-09-13T16:44:04+08:00。报告生成：2026-09-13T16:45:21+08:00。

本实验固定真实权重、初始噪声和观测，比较同一 PR 的 safe 与优化实现。
共用 eager 图像 embeddings；不采集延迟。替换仅发生在诊断进程，不修改上游源码或现有环境。

RoPE 错误的源码与最小 GPU 复现见 [调查说明](../05/README.md)。

模型实际 expert inv_freq dtype：`torch.bfloat16`；prefix inv_freq dtype：`torch.bfloat16`。
权重加载缺失参数：[]。

相对 RMSE 的分母是相同条件下 safe 动作的 RMS；没有真实动作标签。

## legacy_masked_images

图像 mask：[False, False, False]；原始 prefix 长度 968，有效 prefix 长度 150。

| 解码器配置 | safe prefix KV：相对 RMSE | 优化 prefix KV：相对 RMSE | 优化 prefix KV：绝对 RMSE |
| --- | ---: | ---: | ---: |
| 原始优化解码器 | 103.5207% | 103.2644% | 0.05946983 |
| 仅修正 RoPE 维度配对 | 1.5033% | 2.7869% | 0.00160498 |
| 修正配对 + 匹配模型 RoPE 频率 | 1.4509% | 2.8927% | 0.00166589 |
| 仅还原 safe 输出投影 / Euler / FP32 状态 | 103.4740% | 103.2385% | 0.05945492 |
| 修正配对 + 还原 Euler，频率仍为重新计算 | 1.1258% | 2.8725% | 0.00165429 |
| 配对 + 模型频率 + Euler | 1.0994% | 2.8415% | 0.00163643 |
| 配对 + eager RoPE 舍入 + Euler，频率仍为重新计算 | 1.1396% | 2.7535% | 0.00158575 |
| 配对 + 模型频率 + eager RoPE 舍入 + Euler | 1.0779% | 2.7865% | 0.00160474 |

控制组：

- safe 移除被 mask 的 token：相对 RMSE 2.0782%，最大绝对误差 0.00488281。
- safe 预计算条件替代逐步计算：相对 RMSE 0.0000%，最大绝对误差 0.00000000。
- 仅替换前缀编码器，保留 safe 解码器：相对 RMSE 2.7986%，最大绝对误差 0.00683594。

## three_valid_images

图像 mask：[True, True, True]；原始 prefix 长度 968，有效 prefix 长度 918。

| 解码器配置 | safe prefix KV：相对 RMSE | 优化 prefix KV：相对 RMSE | 优化 prefix KV：绝对 RMSE |
| --- | ---: | ---: | ---: |
| 原始优化解码器 | 166.2306% | 166.1589% | 0.22271822 |
| 仅修正 RoPE 维度配对 | 1.9574% | 1.9293% | 0.00258606 |
| 修正配对 + 匹配模型 RoPE 频率 | 0.8338% | 0.9177% | 0.00123009 |
| 仅还原 safe 输出投影 / Euler / FP32 状态 | 166.2185% | 166.2142% | 0.22279239 |
| 修正配对 + 还原 Euler，频率仍为重新计算 | 1.8873% | 1.8628% | 0.00249689 |
| 配对 + 模型频率 + Euler | 0.7522% | 0.8129% | 0.00108956 |
| 配对 + eager RoPE 舍入 + Euler，频率仍为重新计算 | 1.8936% | 1.8974% | 0.00254327 |
| 配对 + 模型频率 + eager RoPE 舍入 + Euler | 0.7000% | 0.8292% | 0.00111148 |

控制组：

- safe 移除被 mask 的 token：相对 RMSE 0.6963%，最大绝对误差 0.00488281。
- safe 预计算条件替代逐步计算：相对 RMSE 0.0000%，最大绝对误差 0.00000000。
- 仅替换前缀编码器，保留 safe 解码器：相对 RMSE 0.7929%，最大绝对误差 0.00512695。

## 历史结果复核

历史采集属于 2026-09-13 的 baseline_retry；本次采集时间见文首，二者分别保留。
初始噪声逐元素相同：True。
本次完整 safe 与历史 safe 最大绝对差：0.00488281。
本次原始优化路径与历史 optimized 最大绝对差：0.00341797。

## 解释边界

这些是固定种子合成观测下的数值消融；不同改动存在交互，误差变化不能简单相加为归因百分比。
匹配 safe 的 BF16 RoPE 频率用于内部对齐；对外部模型正确性仍需另行核对 OpenPI/LeRobot。
即使相对误差下降，也不自动等于满足部署容差。原历史延迟不能用于这里改变后的计算路径。

原始指标与逐层 prefix KV 差异：`numerics.json`；动作与逐步轨迹：`actions_and_traces.npz`。
实际脚本快照：`diagnose_numerics.source.py`；加载与运行日志：`run.log`。
