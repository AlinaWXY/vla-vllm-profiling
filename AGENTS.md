# CHIA 工作区规则

以下规则适用于本工作区中的所有代理，包括子代理。除非用户明确覆盖具体规则，否则必须遵守。

## 存储

- `/fact_home` 仅用于 dotfiles、SSH 密钥和配置；不得存放源码、检查点、数据集、实验输出或其他数据。
- `/fact_data` 用于源码、检查点和实验数据。
- `/scratch` 仅用于可丢弃的临时文件和缓存，包括可重新下载的模型权重和数据集；不得存放重要或不可替代的数据。
- 不得在 home 目录存放数据。
- 不得向 `/tmp` 写入数据；仅允许用于不产生持久数据的、真正临时的命令衔接操作。
- 不得将 `local/data/` 用于任何用途。

## 文件系统搜索

- 禁止使用 `find`。
- 禁止大范围递归扫描，例如 `ls -R`、对大目录运行 `rg` 或 `grep -r`。
- 禁止扫描 home 目录或文件系统根目录，尤其不得运行 `find ~`。
- 优先进行限定范围的非递归检查。需要搜索仓库时，使用 `rg --files` 或 `rg`，并明确限定路径和文件 glob。

## 作业调度

- Thor 例外（用户于 2026-09-12 明确授权）：通过 `ssh thor0` 连接的 Thor 主机
  （当前 hostname 为 `fact-thor`）可直接运行 GPU/CPU 作业，不需要 Slurm。
- 其他机器上使用 GPU 或大规模 CPU 核心的作业仍必须通过 Slurm 提交和调度。

## Thor 环境复用

- 优先复用 Thor 和共享 `/fact_data` 中已有的环境、框架及模型文件。
- 用户于 2026-09-12 明确授权：在其他机器准备面向 Thor 的 ARM64 环境，
  存放到共享 `/fact_data`，由 Thor 运行；需源码编译的 CUDA 代码以 `sm_110a` 为目标。
- 不在 Thor 安装、升级依赖或构建软件；不修改任何机器的现有环境。
  新环境使用项目独立目录，下载和构建缓存放 `/scratch`。
- 上一条的构建限制指环境/框架安装；推理必要的 Triton/torch.compile JIT
  可以在 Thor 运行，限制编译并发并将缓存放在 `/scratch`。
- 用户于 2026-09-13 授权自行下载缺少的模型/tokenizer；在非 Thor 机器下载到
  `/scratch`，固定来源、版本并校验完整性。Thor 推理仍保持本地离线读取。
- 检查环境时使用只读查询；不修改 sudo/Docker 权限，不自动启动或重启容器。

## 实验和绘图记录

- 用户于 2026-09-13 要求：每次实验／重新绘图使用当天独立编号，例如
  `results/processed/0913/00`、`0913/01`，按 Asia/Hong_Kong 日期递增，不覆盖旧图。
- 记录含年份和时区的实际采集开始／结束时间、绘图时间、输入文件及实验条件。
  重画历史数据必须分别标明原采集时间和本次绘图时间，不能把重画时间当作采集时间。
- 使用 `python -m profiling.experiments --purpose ...` 分配编号；图内标注编号和绘图时间，
  同目录保存实验说明、数据来源和元数据。
- 用户后续于 2026-09-13 明确选择：暂不修复／阻塞于 PR4419 的 RMSE 差异，继续
  VLM、Action Expert 的逐算子及整体 profiling，并绘制完整 VLA 的整体 roofline。
  对当前实验实现测量，不声称数值等价加速；必须校验实际图像 masks 和前缀长度。
  真实整体时长独立采集，不用逐 kernel NCU 时间之和冒充整体推理时长。
- 用户随后要求改用另一任务准备的新 Omni 框架。当前选择
  `/fact_data/xinyaowang/vllm-omni-pi05-numerical-fixes` 的 `6bdbf97`；
  新实验记录实际源码提交和文件哈希，不能继续将旧 PR 的结果标为新框架结果。

## 委派

- 向任何子代理分配文件系统、存储、下载或实验任务之前，必须告知以上规则。
- 子代理继续委派时，也必须传递同样的约束。
- 本节仅规定已获授权的委派如何传递规则，不单独授权启动子代理。

## 当前 GitHub 发布范围（2026-09-14）

用户要求只保留最后认可的两组 L20 DRAM Roofline：`results/processed/0914/15`（Omni）和 `results/processed/0914/16`（SGLang）。历史实验在本地归档和 Git 历史中追溯；当前发布目录不重新加入旧结果。两组各自的 `capture/` 为采集依据，必须保留测量/来源一致性。新重绘继续分配当日独立编号，保留原采集时间。
